namespace Mockifyr.Differential.Generator;

// Placeholder for the deterministic property-based / fuzzing input generator.
// Given a seed, it produces structurally-valid requests (header/query/body permutations,
// boundary values: empty, long, unicode, array-order, missing/extra fields) so the same
// input can be replayed against both Mockifyr and the WireMock oracle. Built at G0.
// See docs/decisions/0002-differential-oracle-wiremock.md.
