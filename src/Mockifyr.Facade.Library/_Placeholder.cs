namespace Mockifyr.Facade.Library;

// Placeholder for the Mockifyr.Facade.Library assembly.
// The in-process public API (direct-call mode, used inside tests — the .NET analog of
// WireMock's "no HTTP server" mode) is added as the vertical matures.
