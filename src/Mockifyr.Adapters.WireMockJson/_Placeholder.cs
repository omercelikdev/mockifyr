namespace Mockifyr.Adapters.WireMockJson;

// Placeholder for the Mockifyr.Adapters.WireMockJson assembly.
// The WireMock JSON <-> domain model import adapter is added at roadmap item G0;
// it is itself under differential test (the harness loads WireMock JSON through it).
// See docs/decisions/0004-own-model-wiremock-adapter.md.
