namespace Mockifyr.ServeEvents.Webhook;

// Placeholder for the Mockifyr.ServeEvents.Webhook assembly.
// The IServeEventListener webhook implementation (async outbound HTTP + templating,
// originalRequest correlation) is added at roadmap items G3a/G3b. Outbound I/O lives
// here, outside the pure engine core. See docs/decisions/0001-transport-agnostic-core.md.
