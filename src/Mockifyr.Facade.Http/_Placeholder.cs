namespace Mockifyr.Facade.Http;

// Placeholder for the Mockifyr.Facade.Http assembly.
// The Kestrel-based mock HTTP server lands here: ITenantResolver (subdomain/path/header),
// transport -> CanonicalRequest mapping, and wire-affecting delivery behaviors
// (CORS, gzip, chunked encoding, host-header). It applies the engine's delay/fault/proxy
// directives. See docs/decisions/0001 and ARCHITECTURE.md section 7.
